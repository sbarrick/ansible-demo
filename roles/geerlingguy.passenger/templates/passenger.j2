server {
  listen 80 default_server;
  server_name {{ passenger_server_name }};
  passenger_enabled on;
  passenger_app_env {{ passenger_app_env }};
  passenger_env_var {{ redis_env_var }} {{ redis_requirepass }};
  root {{ passenger_app_root }};
}
